# 프로덕션 이미지에서는 NEXT_PUBLIC_* 값이 빌드 시점에 고정됩니다.
# 백엔드 API 주소 (back 레포 실행 주소)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# 개발 중 로그인 없이 역할 bypass 허용 여부 (true | false)
NEXT_PUBLIC_DEV_ROLE_BYPASS=false
